import { getRepairOrderDetail } from "@/data/repair-orders";
import { getInspectionForRepairOrder } from "@/data/inspections";
import { listQuotationsForRepairOrder } from "@/data/quotations";
import { requireStaffPermissionPage } from "@/features/auth/guards";
import { saveInspectionFormAction, startInspectionFormAction } from "@/features/inspections/actions";
import { saveQuotationDraftFormAction, sendQuotationFormAction } from "@/features/quotations/actions";
import { repairOrderStatusLabel } from "@/features/repair-orders/labels";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";

export default async function RepairOrderDetailPage({ params }: { params: Promise<{ id: string }> }) {
  const { garageId } = await requireStaffPermissionPage("/lenh-sua-chua", "repair-order:read");
  const order = await getRepairOrderDetail(garageId, (await params).id);
  const [inspection, quotations] = await Promise.all([getInspectionForRepairOrder(garageId, order.id), listQuotationsForRepairOrder(garageId, order.id)]);
  const checklist = order.intakeChecklist && typeof order.intakeChecklist === "object" ? order.intakeChecklist as Record<string, unknown> : {};
  return <div className="space-y-6"><div><div className="flex items-center gap-3"><h1 className="font-mono text-2xl font-semibold">{order.code}</h1><Badge variant="secondary">{repairOrderStatusLabel(order.status)}</Badge></div><p className="text-muted-foreground mt-1 text-sm">{order.vehicle.licensePlate} · {order.customer.name}</p></div><Card><CardHeader><CardTitle>Tiếp nhận xe</CardTitle></CardHeader><CardContent className="grid gap-3 text-sm sm:grid-cols-2"><p><span className="text-muted-foreground">Số km:</span> {order.mileageKm?.toLocaleString("vi-VN") ?? "Chưa ghi"}</p><p><span className="text-muted-foreground">Nhiên liệu:</span> {order.fuelLevel === null ? "Chưa ghi" : `${order.fuelLevel}%`}</p><p className="sm:col-span-2"><span className="text-muted-foreground">Ghi chú:</span> {order.initialNote ?? "Không có"}</p><p className="sm:col-span-2"><span className="text-muted-foreground">Checklist:</span> {Object.entries(checklist).filter(([, value]) => value).map(([key]) => key).join(", ") || "Chưa đánh dấu"}</p></CardContent></Card><Card><CardHeader><CardTitle>Kiểm tra xe</CardTitle></CardHeader><CardContent>{inspection ? <form action={saveInspectionFormAction} className="grid gap-3"><input type="hidden" name="repairOrderId" value={order.id}/><textarea name="summary" defaultValue={inspection.summary ?? ""} placeholder="Tổng kết kiểm tra" className="min-h-20 rounded-md border p-2"/><div className="grid gap-2 sm:grid-cols-2"><input name="category" placeholder="Hệ thống" required className="rounded-md border p-2"/><input name="name" placeholder="Hạng mục" required className="rounded-md border p-2"/><select name="severity" className="rounded-md border p-2"><option value="OK">Bình thường</option><option value="ATTENTION">Cần theo dõi</option><option value="URGENT">Khẩn cấp</option></select><input name="finding" placeholder="Phát hiện" className="rounded-md border p-2"/></div><input name="recommendation" placeholder="Khuyến nghị" className="rounded-md border p-2"/><Button type="submit">Lưu kiểm tra</Button></form> : <form action={startInspectionFormAction}><input type="hidden" name="repairOrderId" value={order.id}/><Button type="submit">Bắt đầu kiểm tra</Button></form>}</CardContent></Card><Card><CardHeader><CardTitle>Báo giá</CardTitle></CardHeader><CardContent className="space-y-4">{order.status === "INSPECTING" && <form action={saveQuotationDraftFormAction} className="grid gap-2"><input type="hidden" name="repairOrderId" value={order.id}/><input name="description" required placeholder="Hạng mục" className="rounded-md border p-2"/><div className="grid grid-cols-3 gap-2"><input name="quantity" type="number" min="1" defaultValue="1" className="rounded-md border p-2"/><input name="unitPrice" type="number" min="0" placeholder="Đơn giá" className="rounded-md border p-2"/><input name="discountAmount" type="number" min="0" defaultValue="0" className="rounded-md border p-2"/></div><Button type="submit">Tạo báo giá nháp</Button></form>}{quotations.map((quotation) => <div key={quotation.id} className="rounded-md border p-3"><div className="flex justify-between"><span>Phiên bản {quotation.versionNo}</span><Badge variant="secondary">{quotation.status}</Badge></div><p className="mt-1 font-medium">{quotation.totalAmount.toLocaleString("vi-VN")} đ</p>{quotation.status === "DRAFT" && <form action={sendQuotationFormAction} className="mt-2"><input type="hidden" name="quotationId" value={quotation.id}/><Button type="submit" size="sm">Gửi khách duyệt</Button></form>}</div>)}</CardContent></Card></div>;
}
